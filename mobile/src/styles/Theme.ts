import { ThemeColors } from './ThemeColors';
import { FontFamily, ThemeFonts } from './ThemeFonts';
import { TextSize, ThemeFontSize } from './ThemeFontSize';
import { ThemeSize } from './ThemeSize';

export const Theme = {
  colors: ThemeColors,
  fonts: ThemeFonts,
  fontSize: ThemeFontSize,
  size: ThemeSize,
  textSize: TextSize,
  fontFamily: FontFamily,
};
